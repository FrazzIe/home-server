- name: Get container info
  docker_container_info:
    name: "{{ target_container }}"
  register: result

- name: Restart container
  docker_container:
    name: "{{ target_container }}"
    state: started
    restart: true
  when: result.exists